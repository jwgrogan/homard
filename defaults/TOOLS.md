# Tool Notes

## Shell
- Default shell is zsh on macOS
- Homebrew is at /opt/homebrew on Apple Silicon

## Web
- web_search uses DuckDuckGo (no API key needed)
- web_fetch strips HTML tags for readability
